# 奥特曼投票策略仿真器操作说明

## 1. 程序用途

本程序用于演示以下投票机制可能产生的票型：

- 七名角色分别获得随机的自然票。
- 赛罗不接收“路人策略票”。
- 路人票根据赛罗附近的实时排名，按设定比例投给其他角色。
- 路人票量先由起始倍数增长至峰值倍数。
- 赛罗达到指定名次后，路人票量再下降至最终倍数。

本程序只证明某组规则能否产生某种票型，不能据此认定现实投票由特定个人、组织、自动化程序或平台实施。

## 2. 运行与分享

### 自己运行

1. 找到 `ultraman-vote-simulator-share.html`。
2. 双击该文件。
3. 使用 Chrome、Edge、Firefox 等现代浏览器打开。
4. 点击“运行”开始仿真。

程序是单个 HTML 文件，不需要安装 Python、Node.js 或其他软件，也不需要联网。

### 发给别人

可以直接发送 `ultraman-vote-simulator-share.html`。

如果聊天软件禁止发送 HTML 文件，请发送压缩包：

`ultraman-vote-simulator-share-20260727.zip`

接收者解压后，双击其中的 HTML 文件即可运行。

## 3. 基本操作

### 运行

点击“运行”，程序会持续按秒模拟。按钮随后变成“暂停”，再次点击可暂停。

### 单步

点击“单步”，只模拟一秒，适合观察单次票数分配和名次变化。

### 按当前参数重置

修改参数后，必须点击“按当前参数重置”，新参数才会载入。

该操作会：

- 停止当前仿真；
- 恢复输入框中的初始票数；
- 清空历史曲线；
- 使用当前随机种子重新开始。

### 播放速度

播放速度只影响画面播放快慢，不改变每个模拟秒内的投票算法和最终结果。

## 4. 票量与阶段参数

### 初始倍数

仿真开始时：

`路人票期望值 = 赛罗自然票率 × 初始倍数`

默认值为 1 倍。

### 峰值倍数

增长阶段结束后的最高路人票倍数。默认值为 10 倍。

### 最终倍数

下降阶段结束后持续保持的路人票倍数。默认值为 4 倍。

### 升至峰值用时

路人票由初始倍数线性增长至峰值倍数所需的模拟秒数。默认值为 60 秒。

### 开始下降的赛罗名次

只有同时满足以下条件，才会触发下降：

1. 已经完成增长阶段；
2. 赛罗达到或低于设定名次。

默认值为第 7 名，即赛罗首次垫底后开始下降。

### 下降用时

从峰值倍数线性下降至最终倍数所需的模拟秒数。默认值为 60 秒。

### 随机种子

随机种子用于复现实验。

当全部参数和随机种子相同时，重置后应得到相同的随机投票过程。修改随机种子，可以观察同一策略在不同随机波动下的结果。

### 图表窗口

控制曲线显示最近多少个模拟秒，不改变仿真计算。

## 5. 初始票数与自然票率

### 初始票数

每名角色在第 0 秒的票数。可以用它模拟“赛罗最初第一”或其他起始排名。

### 自然票率

表示每名角色每秒自然获得票数的期望值。

程序使用泊松随机过程生成整数票数，因此某一秒实际增票可能高于或低于输入值。例如自然票率为 1.5 票/秒，并不表示每秒固定增加 1.5 票，而是长期平均约为 1.5 票/秒。

赛罗的自然票率还作为路人票倍数的计算基准。

## 6. 路人票分配规则

所有位置都以赛罗的当前名次为基准：

- 前一名：紧邻赛罗、排名比赛罗高一位的角色。
- 后一名：紧邻赛罗、排名比赛罗低一位的角色。
- 前方其余：领先赛罗，但不包括前一名的其他角色。
- 后方其余：落后赛罗，但不包括后一名的其他角色。

### 赛罗未垫底时的默认权重

- 前一名：40%
- 后一名：40%
- 前方其余：10%
- 后方其余：10%

### 赛罗垫底后的默认权重

- 前一名：40%
- 后一名：20%
- 前方其余：35%
- 后方其余：5%

### 不存在某一组时如何处理

四项输入是大型投票中的固定分流权重，不会因为当前七人子集缺少某一组而重新归一化。

如果某一方向没有本子集角色，分配到该组的票视为投向大型投票中的其他角色，不计入当前七人子集。

例如赛罗真正垫底时，不存在“后一名”和“后方其余”。按照垫底后的默认权重：

- 前一名：40%进入当前子集；
- 前方其余：35%进入当前子集；
- 后一名：20%流向子集外；
- 后方其余：5%流向子集外。

因此平均只有75%的路人票进入当前七人子集，另外25%在本程序中记为“未进入本子集”。

如果某组包含多名角色，该组获得票后，会在组内角色之间随机选择一名。

程序在每个模拟秒开始时读取并冻结一次排名。本秒先按四项权重计算整数配额：前一名、后一名分别获得自己的固定配额；“前方其余”和“后方其余”只在各自组内随机分配。角色要到下一秒开始时才会重新确定所属分组。

## 7. 页面指标

### 路人票／赛罗自然票

显示当前路人票期望值相对于赛罗自然票率的倍数。

### 赛罗名次

显示赛罗当前在七名角色中的排名，并显示下降是否已经触发。

### 赛罗前方奥特曼最大票差

计算公式为：

`赛罗前方最高票角色票数 − 赛罗前一名票数`

计算中不包括赛罗本人。

如果赛罗前方不足两名角色，结果显示为 0。

该指标衡量赛罗前方角色群内部从最高票到紧邻赛罗者之间的跨度，而不是最高票角色领先赛罗本人的票数。

## 8. 排名条形图

- 条形长度表示当前票数的相对大小。
- 赛罗使用单独颜色显示。
- 本秒获得最多路人票的角色会被突出显示。
- 左侧数字为实时名次。
- 右侧显示“当前总票数（+上一模拟秒增量）”，例如 `1,234（+5）`。

## 9. 曲线图

- 纵坐标：各奥特曼当前真实票数。
- 六条细线：六名其他奥特曼各自的真实票数。
- 粗线：赛罗的真实票数。
- 竖线标记峰值阶段、触发下降和到达最终倍数的时刻。

## 10. 推荐实验方法

### 实验一：验证路人票是否足够改变排名

1. 保持默认初始票数和自然票率。
2. 把峰值倍数改为不同数值，例如 4、6、8、10。
3. 每次修改后点击“按当前参数重置”。
4. 使用同一随机种子运行。
5. 比较赛罗何时失去第一、何时垫底。

### 实验二：验证邻近投票是否形成“推墙”

1. 保持路人票总量不变。
2. 提高“前一名”和“后一名”的权重。
3. 降低“前方其余”和“后方其余”的权重。
4. 观察票数是否更集中在赛罗附近，名次是否频繁轮换。

### 实验三：验证最终 4 倍是否能长期维持

1. 让赛罗垫底后路人票下降至 4 倍。
2. 延长运行时间。
3. 比较赛罗自然票率与其他角色的平均自然票率。
4. 观察赛罗是否逐渐追回前方角色。

### 实验四：检验随机波动

1. 固定所有参数。
2. 分别使用多个随机种子。
3. 记录赛罗垫底时间、最大票差和最终排名。
4. 不要只用一次运行结果判断策略是否稳定。

## 11. 结果解释注意事项

- 单次仿真只能说明一种可能路径。
- 参数组合能够复现某种票型，不代表现实一定使用了该策略。
- 多种机制可能产生相似的公开票数曲线。
- 若要区分人工、自动化或平台调节，需要结合高频时间序列、响应延迟、过冲、账号行为和后台日志。
- 应同时进行多次随机种子实验，并报告结果分布，而不是只展示最符合预期的一次。

## 12. 常见问题

### 修改参数后结果没有变化

修改后需要点击“按当前参数重置”，再点击“运行”。

### 为什么同一参数每次结果不同

检查随机种子是否相同。若种子相同且其他参数完全一致，结果应可复现。

### 为什么路人票没有全部进入七人子集

当前仿真只是大型投票的子集。若抽中的分组在本子集中没有角色，该票会流向子集外，不会重新分配给剩余角色。页面会显示本秒“未进入本子集”的票数。

### 为什么自然票不是固定增加

自然票按照随机过程生成，输入值是长期平均速率，不是每秒固定票数。

### 为什么赛罗垫底后仍可能追回

如果最终阶段中，其他角色平均获得的自然票加路人票低于赛罗自然票，赛罗长期仍可能追回。

### 页面没有自动更新为新参数

点击“按当前参数重置”。如果程序文件刚被替换，请刷新浏览器页面。
